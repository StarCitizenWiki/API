# Traefik holds backend connections open; long keepalive pins workers to idle clients.
# A short timeout returns them to the spare pool fast under bursts.
KeepAlive On
KeepAliveTimeout 2
MaxKeepAliveRequests 100
