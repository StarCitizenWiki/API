<VirtualHost *:80>
    CustomLog ${APACHE_LOG_DIR}/access.log combined
    ErrorLog ${APACHE_LOG_DIR}/error.log

    DocumentRoot /opt/api/public

    <Directory "/opt/api/public">
        AllowOverride all
        Require all granted
    </Directory>
</VirtualHost>